import { makeStyles } from '@mui/styles';

export default makeStyles({
    wrapper: {
        display: "flex",
        justifyContent: "center",
        maxWidth: 968,
        marginLeft: "auto",
        marginRight: "auto",
        padding: 24
    },
})